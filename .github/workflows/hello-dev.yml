name: Hello Dev Branch

on:
  push:
    branches: [ dev ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "test vercel deploy"
      - name: Check what time it is
        run: date
